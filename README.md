# Web Work Media — Doctor Website Offer

Static HTML/CSS/JS landing page for doctor website packages.

## Contact & payment
- bKash Send Money: `01751210179`
- WhatsApp/contact supplied for this build: `01302778420`

## GitHub file structure
Keep these files in the repository root:

- `index.html`
- `styles.css`
- `script.js`
- `assets/`

If your repository already has a `CNAME` file for the Hostinger subdomain/custom domain, keep it unchanged.

## Order form
The package button opens a 3-step order form:
1. bKash payment details
2. Doctor profile information
3. Up to 3 chambers

The completed order form opens WhatsApp with all filled-in order details so the customer can send everything quickly.

## V3 purchase-focused changes
- Uses the supplied Web Work Media SVG logo assets.
- Uses all 5 supplied doctor website mockup PNGs in the hero slideshow.
- Mockup slideshow changes automatically and also has manual arrows/dots.
- Pricing is visible inside the hero on first load and the full pricing section appears immediately below the hero.
- Hero price cards can start the package order directly.
